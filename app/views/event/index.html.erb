<h1>Events</h1>

<%= link_to "Create New Event", new_event_path %>
<p>
  <ul>
<% @events.each do |event| %>
  <li> <%= link_to event.name, event_path(event) %> ||  <%= link_to "Score this Event", scorecard_path(event.id) %></li> 
<% end %>
</ul>
</p>